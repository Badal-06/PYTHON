# List of numbers
nums = [1, 4, 9, 16, 25, 36, 49, 64, 81, 100]

# Starting index for iteration
idx = 0

# Loop until index reaches the length of the list
while idx < len(nums):
    # Print the element at the current index
    print(nums[idx])
    
    # Move to the next index
    idx += 1
